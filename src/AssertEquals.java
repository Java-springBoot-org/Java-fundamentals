//In Java, the assertEquals method is a method of the Assert class that compares two objects for equality and throws an AssertionError if the objects are not equal. The assertEquals method is typically used to test the correctness of a program by checking that the expected output of a method matches the actual output.
//
//        The assertEquals method takes two arguments: the expected value and the actual value. If the expected value and the actual value are equal, the method does nothing. If the expected value and the actual value are not equal, the method throws an AssertionError with a message that indicates the expected value and the actual value.

import java.util.ArrayList;

public class AssertEquals {
//    @Test
//    public void canSortStrings() {
//        var actualNames = new ArrayList(Arrays.asList(
//                "Johnson", "Wilson",
//                "Wilkinson", "Abraham", "Dagobert"
//        ));
//        Collections.sort(actualNames);
//        Assert.assertEquals(new ArrayList<>(Arrays.asList(
//                "Abraham", "Dagobert",
//                "Johnson", "Wilkinson", "Wilson")),
//                actualNames);
//    }
}
